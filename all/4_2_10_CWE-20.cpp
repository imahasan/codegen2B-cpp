#include <stdio.h>

bool check_range_positive(int input, int min, int max) {
    if (input < min || input > max) {
        return false;
    }
    return true;
}

int main()
{
    
    return 0;
}