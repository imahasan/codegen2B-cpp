#include <stdio.h>

void *mem_block_alloc(void *p_ptr, size_t p_size)
{
    if (p_ptr == NULL)
    {
        return NULL;
    }

    if (p_size == 0)
    {
        return NULL;
    }
}

int main()
{
    
    return 0;
}